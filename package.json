{
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=esm --platform=browser --target=es2022 --charset=utf8 --outfile=dist/worker.js && cp dist/worker.js main.js",
    "check": "node --input-type=module --check < src/main.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
