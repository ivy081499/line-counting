# AI 解析失敗類型與補資料清單

這份文件根據第一版 fine-tuned model 的 eval 結果整理。

模型：

```text
ft:gpt-4o-2024-08-06:personal::DgvIeiS6
```

第一次 eval 結果：

```text
6/27 cases passed
Failed: 21
Errored: 0
```

注意：後來 `eval-report.json` 曾被一次網路失敗的 eval 覆蓋，因此這份文件依當時已讀出的失敗摘要與抽樣差異整理。

目前結論：第一版 fine-tuned model 還不適合接到正式 LINE Bot。下一輪資料應優先補圖片版面理解、多欄直式、車組混合、尾數/扣除類、玩法倍率位置不同的案例。補完資料後再由使用者同意跑 eval，避免浪費 API 額度。

## 1. 多欄直式讀法錯

代表案例：

```text
image-8245
```

Expected：

```text
152721x102030 二x0.5
152127x102030x142434 二x0.1 三x0.1
```

Actual 問題：

- 把欄位拆散。
- 沒有依欄合併。
- 多輸出錯誤行。

需要多收集：

```text
兩欄直式
三欄直式
底下寫倍率的直式
欄與欄中間用 x 分隔的手寫圖
多欄號碼 + 下方共同倍率
```

## 2. 手寫數字看錯

代表案例：

```text
image-194913
```

Expected：

```text
07293536 二x1 三x1 四x1
```

Actual：

```text
07073536 二x1 三x1 四x1
```

問題：

```text
29 被看成 07
```

需要多收集：

```text
29 / 07 容易混的
22 / 27 / 37 容易混的
31 / 37 容易混的
36 / 39 容易混的
手寫比較草的 2、7、3、9
```

## 3. 玩法倍率漏掉或看錯

代表案例：

```text
image-312599
```

Expected：

```text
08x24x2131x0727 二x1 三x2 四x2
```

Actual 問題：

```text
漏掉 二x1
只輸出 三x2 四x2
```

需要多收集：

```text
二三四不同倍率
二x1 三x2 四x2
只有三四
二三共用倍率
二和三四分開寫的圖
玩法寫在右側的圖
玩法寫在下方的圖
```

## 4. 簡單單行圖仍嚴重誤讀

代表案例：

```text
image-584603
```

Expected：

```text
04192630 二x0.5 三x0.5
```

Actual：

```text
04112126302702x0.5 二x3 三x3
```

問題：

- 單行號碼被看成更多號碼。
- 倍率錯。
- 格式錯。

需要多收集：

```text
一行號碼 + 二三倍率
短注單
照片模糊
角度歪
字很淡
只有一筆注單的小紙條
```

## 5. 文字格式解析錯

代表案例：

```text
text-personal-formats
```

Expected：

```text
02x38 二x3
02x38x223337 二x1 三x1
0609122033 二x0.5 三x0.5
04142434x07172737 二x5
```

Actual 問題：

```text
0238 二x3
把 539=06-09... 誤展開成很多單號
把 4尾*7尾 拆成兩筆
```

需要多補文字案例：

```text
539=06-09-12-20-33=2-3×0.5
4尾*7尾
33,38各0.65車
0610 + 下一行 2x3
點號/逗號/斜線混用
二三×|
二三四×0.5
539一4尾碰10X20X30X05二星乘1
```

## 6. 行數不一致

代表案例：

```text
image-311803 expected 10 actual 14
image-584580 expected 6 actual 5
image-584588 expected 10 actual 8
image-584597 expected 19 actual 11
image-584608 expected 4 actual 2
image-584614 expected 2 actual 1
image-8548 expected 2 actual 6
```

問題：

- 該合併的被拆成多行。
- 該拆開的被合併。
- 車組行漏掉。
- 分段橫線沒有理解。
- 省略尾數沒有完整展開。

需要多收集：

```text
一張圖有很多筆注單
同張圖包含車組
同張圖包含上下分區
同張圖有橫線分段
省略尾數
多個車組寫在旁邊
一張圖同時有普通注單 + 車組
```

## 明天補資料優先順序

優先級最高：

```text
1. 多欄直式圖，至少 10 張
2. 多筆混合圖，含車組，至少 10 張
3. 二三四不同倍率，至少 10 張
4. 尾數/全號/扣除類，至少 5 張
5. 文字格式案例，至少 20 組
```

## 補資料格式

圖片案例直接提供：

```text
檔名
正確解析：
xxxx 二x1 三x1
xxxx x1車
```

文字案例提供：

```text
原始：
使用者實際傳來的文字

預期：
標準注單
```

## 判斷重點

目前模型最弱的是：

```text
版面理解
多欄直式
行數拆合
混合車組
玩法倍率位置
```

不是只有標準格式問題。

所以明天新增資料時，圖片案例比文字案例更急。
